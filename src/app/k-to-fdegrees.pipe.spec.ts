import { KToFDegreesPipe } from './k-to-fdegrees.pipe';

describe('KToFDegreesPipe', () => {
  it('create an instance', () => {
    const pipe = new KToFDegreesPipe();
    expect(pipe).toBeTruthy();
  });
});
